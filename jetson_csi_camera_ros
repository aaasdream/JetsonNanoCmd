sudo apt-get install ros-${ROS_DISTRO}-cv-bridge
sudo apt-get install ros-${ROS_DISTRO}-vision-opencv

cd ~/catkin_ws/src
git clone https://github.com/sfalexrog/jetson_camera
cd ~/catkin_ws
catkin_make
source devel/setup.bash
